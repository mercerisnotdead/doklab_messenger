# Простой мессенджер: консоль, GUI и веб

Варианты:
- **TCP сервер** на `asyncio` (stdlib)
- **GUI-клиент** на `tkinter` к TCP-серверу
- **WebSocket сервер** на `websockets`
- **Браузерный клиент** (HTML+JS)

## 1) TCP чат (консоль)
```bash
python tcp_server.py --host 0.0.0.0 --port 8888
```
Клиенты: запустить `client.py` из прошлой версии или GUI ниже.

## 2) GUI-клиент (tkinter) для TCP
```bash
python tkinter_client.py --host 127.0.0.1 --port 8888 --name Alice
```

## 3) WebSocket-чат (сервер)
Установите зависимость:
```bash
pip install websockets
```
Запуск:
```bash
python websocket_server.py --host 0.0.0.0 --port 8765
```

## 4) Веб-клиент
Откройте файл `public/index.html` в браузере, либо подключитесь к серверу по адресу
`ws://<host>:8765`. По умолчанию кнопка "Подключиться" использует `ws://localhost:8765`.

> Примечание: браузер **не может** подключаться к TCP-серверу напрямую — только к WebSocket.
> Поэтому для браузера предусмотрен отдельный WebSocket-сервер.

## Подсказки
- Команда `/quit` завершает соединение.
- Эмодзи: 🟢 вход, 🟡 выход, 💬 сообщения.
- Порты настраиваются через аргументы команд.